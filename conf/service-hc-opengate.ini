[Unit]
Description=hc-opengate
After=syslog.target network-online.target

[Service]
Type=simple
User=root
WorkingDirectory=/var/lib/hc-opengate
EnvironmentFile=/etc/default/hc-opengate.env
ExecStart=/usr/local/bin/hc-http-switch \
  --name="OpenGate" \
  --pin="${OPENGATE_PIN}" \
  --url="${URL_CHECKGATE}" \
  --url="${URL_OPENGATE}" \
  --duration=0 \
  --manufacturer="NARAN" \
  --model="MicroBot Push" \
  --firmware="${OPENGATE_FIRMWARE}"

Restart=on-failure
RestartSec=10
KillMode=process

[Install]
WantedBy=multi-user.target
